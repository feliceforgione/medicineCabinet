services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "8080:80"
    container_name: medicine-cabinet-frontend
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
